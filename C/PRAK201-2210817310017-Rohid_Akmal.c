#include <stdio.h>

int main(){
    char nm[50], nim[50], kp[50], ttl[50], al[50], hb[50], hp[50];

    printf("Nama                    :");
    gets(nm);
    printf("NIM                     :");
    gets(nim);
    printf("Kelas Pararel           :");
    gets(kp);
    printf("Tempat/Tanggal Lahir    :");
    gets(ttl);
    printf("Alamat                  :");
    gets(al);
    printf("Hobby                   :");
    gets(hb);
    printf("No. HP                  :");
    gets(hp);

    return 0;
}